# Environment variables accessible to Vite must start with VITE_
# Example: Base URL for your authentication API
VITE_API_BASE_URL=https://api.example.com/v1

# Example: A configured timeout for requests
VITE_REQUEST_TIMEOUT=5000

# WARNING: Do not store highly sensitive secrets (like private keys) here if they are not meant to be exposed to the browser.
